# Get a key at https://console.anthropic.com. Without it the app runs in demo
# mode over a fixed post and fixed search results; the grading and verdicts are
# still the real code.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-opus-5.
ANTHROPIC_MODEL=claude-opus-5
